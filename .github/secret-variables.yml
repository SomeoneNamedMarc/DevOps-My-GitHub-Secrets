name: Upload to FTP Server

on:
  push:
    branches:
      - main

jobs:
  integration:
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch all history for accurate results of code analysis

      - name: Install required tools
        run: |
          dotnet tool install -g dotnet-sonarscanner
          dotnet tool install -g dotnet-reportgenerator-globaltool
          
      - name: Start Static Code Analysis
        run: | # INSERT RELEVANT VARIABLES BELOW
          dotnet sonarscanner begin \
            /k:"---PROJECT-KEY-GOES-HERE---" \ 
            /d:sonar.host.url="---URL-TO-SONAR-GOES-HERE---" \
            /d:sonar.token="---SONAR-TOKEN-GOES-HERE---" \
            /d:sonar.coverageReportPaths="coverage-report/SonarQube.xml" \
            /d:sonar.qualitygate.wait=true \
            /d:sonar.qualitygate.timeout=300

        - name: Build
          run: dotnet build

        - name: Run unit tests
          run: dotnet test --collect:"XPlat Code Coverage" --results-directory TestResults

        - name: Generate report
          run: |
            reportgenerator \
            -reports:TestResults/**/coverage.cobertura.xml \
            -targetdir:coverage-report \
            -reporttypes:SonarQube

        - name: Complete Static Code Analysis
          run: | # INSERT RELEVANT VARIABLES BELOW
            dotnet sonarscanner end \
              /d:sonar.token="---SONAR-TOKEN-GOES-HERE-AGAIN---"
